import { EtherpadConfiguration } from './types';
export declare const defaultConfiguration: EtherpadConfiguration;
export declare const messages: {
    noConfig: string;
    noApiKey: string;
    invalidApiKey: string;
    invalidUrl: string;
    invalidVersion: string;
};
export default function getConfiguration(config: EtherpadConfiguration): EtherpadConfiguration;
